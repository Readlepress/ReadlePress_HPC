# AGENTS.md

## Cursor Cloud specific instructions

This repository (`ReadlePress_HPC`) is currently a greenfield project containing only a LICENSE file. There is no application code, no dependency manifests, no build system, and no tests yet.

### Current state

- **Language/framework**: Not yet determined.
- **Package manager**: None configured.
- **Build system**: None configured.
- **Tests**: None exist.
- **Services**: None to run.

### When code is added

Once the first application code is committed, update this section with:

- How to install dependencies (e.g. `npm install`, `pip install -r requirements.txt`).
- How to run the dev server.
- How to run lint checks and tests.
- Any non-obvious environment requirements or gotchas.
