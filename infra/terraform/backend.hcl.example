# S3 Backend Configuration
# Rename to backend.hcl and run: terraform init -backend-config=backend.hcl

bucket         = "readlepress-terraform-state"
key            = "readlepress/dev/terraform.tfstate"
region         = "ap-south-1"
dynamodb_table = "readlepress-terraform-locks"
encrypt        = true
